# ───────────────────────────────────────────────────────────
# ① 비밀/키 (절대 커밋 금지 — 개인키는 repo가 아니라 ~/.ssh 에)
# ───────────────────────────────────────────────────────────
*.ppk
*.pem
*.key
id_*
*_rsa
*_ed25519
.env
*.env
.envrc

# ───────────────────────────────────────────────────────────
# ④ 데이터·DB·로그 (생성물/대용량)
# ───────────────────────────────────────────────────────────
data/
captured/
logs/
*.db
*.db-wal
*.db-shm
*.log

# ───────────────────────────────────────────────────────────
# ② 파이썬 생성물·캐시
# ───────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg-info/
.venv/
build/
dist/

# ───────────────────────────────────────────────────────────
# ② Node / 슬라이드 빌드 임시
# ───────────────────────────────────────────────────────────
node_modules/
package-lock.json
.slides_build*/
~$*.pptx
~$*.docx

# ───────────────────────────────────────────────────────────
# ③ OS / 에디터 잡파일
# ───────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~
.idea/
.vscode/

# ───────────────────────────────────────────────────────────
# GitHub 미반영 파트  (주석은 반드시 줄 단독 — 인라인 # 는 패턴으로 취급됨!)
# ───────────────────────────────────────────────────────────
# 옛 클라우드 문서(도커·멀티옵션·PuTTY)
legacy/
# 학습 파트(별도 관리) — 운영 repo에는 제외

# Claude Code 로컬 설정(머신별)
.claude/settings.local.json

# (커밋 유지: tests/fixtures/*.xml, *.pptx 산출물, *.build.js, pyproject.toml, docs/, *.md)
