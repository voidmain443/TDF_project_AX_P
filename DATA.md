# 데이터 모델 & 크롤링 동작 방식

이 문서는 **데이터가 어디서 오는지 / 어떻게 수집되는지 / 어떻게 쌓이는지 / 각 컬럼이
무엇인지**를 설명합니다.

---

## 1. 데이터 출처 (KOFIA 전자공시)

사이트: `https://dis.kofia.or.kr` (WebSquare + Proframe 구조)
화면이 백엔드 게이트웨이에 **XML POST**를 보내고 응답 XML을 그려줍니다.

| 우리 피드 | KOFIA 공시화면 | 서비스ID(`pfmSvcName`) | 갱신 주기 |
|---|---|---|---|
| 기준가·설정액·순자산 | 펀드기준가격 | `DISFundStdPriceSO` | **매 영업일** |
| 합성총보수(TER)·운용·판매보수 | 펀드별 보수비용비교 | `DISFundFeeCmsSO` | **매월**(월말, 1~2개월 시차) |
| 자금 유출입(순유입) | (위 설정액에서 **파생 계산**) | — | 매 영업일(전일 대비 Δ설정액) |

- **게이트웨이 URL**: `https://dis.kofia.or.kr/proframeWeb/XMLSERVICES/`
  (대문자 **W**. 소문자 `proframeweb`는 죽은 레거시 OpenAPI로 307 리다이렉트 → 이게 과거 "0건" 원인이었음)
- **요청 본문**: `<DISCondFuncDTO><tmpV30>기준일(YYYYMMDD)</tmpV30><tmpV11>운용사코드</tmpV11></DISCondFuncDTO>`
  - `tmpV11`을 **빈 값**으로 두면 **전체 운용사**의 모든 펀드가 한 번에 옵니다(약 26,000행, ~36MB).
- **응답 형식**: 각 행이 `<selectMeta>`, 컬럼은 위치 기반 `tmpV1, tmpV2, …`(아래 매핑 표 참조).

> 서비스ID·컬럼 위치는 `tdf_crawler/config.py` 한 곳에 모여 있어, KOFIA가 구조를 바꾸면
> 거기만 고치면 됩니다. (`python -m tdf_crawler.capture` 로 픽스처 재캡처)

---

## 2. 크롤링 동작 (하루 1회 실행 기준)

`python -m tdf_crawler.run` 한 번 실행 = **그 날짜(`base_date`)의 스냅샷 1개**를 적재:

```
1. 펀드기준가격 1회 벌크 호출        → 전체 26,000여 펀드의 기준가/설정액/순자산
2. 펀드명에 TDF 키워드 필터          → TDF 약 1,950개 클래스만 남김
3. funds(메타) + nav_daily(당일값) upsert
4. 자금유출입 파생: net_flow = (오늘 설정액 − 그 펀드의 직전 적재일 설정액) → flows_daily upsert
5. 보수 1회 벌크 호출(가장 최근 데이터 있는 월말로 자동 백워크) → fees upsert (TDF만)
6. crawl_runs 에 실행 이력 기록
```

키워드: `TDF`, `Target Date`, `타겟데이트`, `타깃데이트` (`config.TDF_NAME_KEYWORDS`).

---

## 3. ⭐ 어떻게 쌓이는가 — "매일 하루치 스냅샷을 누적"

**전체 DB를 매일 갈아엎는 게 아닙니다.** 매 실행마다 **그 날짜 한 칸(base_date)** 이
시계열로 **추가**됩니다. 과거 날짜는 그대로 보존됩니다.

- 테이블 PK = `(fund_code, base_date)`. 즉 **펀드 × 날짜** 한 칸이 한 행.
- 매일 실행하면 `2026-06-04`, `2026-06-05`, … 처럼 **날짜가 한 줄씩 늘어남** → 시계열 축적.
- **같은 날짜를 다시 실행해도** PK 충돌 → `ON CONFLICT DO UPDATE`(upsert)로 **그 날짜 행만 값 갱신**,
  중복 행이 생기지 않음 ⇒ 하루에 여러 번 돌려도, 누락분을 나중에 보충해도 안전.
- 과거 특정일 백필: `--date 2026-06-02` 처럼 날짜를 지정해 그 날 스냅샷만 채워 넣을 수 있음.

예) 06-02와 06-04를 적재한 실제 결과:
```
nav_daily : 3900 행  (= 1950 펀드 × 2 날짜)
distinct base_date : ['2026-06-02', '2026-06-04']
flows_daily(06-04) : net_flow = 설정액(06-04) − 설정액(06-02)
```

요약: **"매일 전체 TDF의 그날 스냅샷 1장을 찍어 시계열로 누적"** 하는 구조입니다.
(증분 컬럼 업데이트가 아니라, 날짜별 전체 스냅샷 누적)

### 과거 전체 백필 (`python -m tdf_crawler.backfill`)
- 시작일~종료일의 **영업일마다** 기준가 스냅샷, **월말마다** 보수를 받아 누적합니다.
- 과거→현재 순서로 적재 후, 마지막에 `recompute_all_flows`로 **전체 날짜의 순유입을 일괄 재계산**
  (먼저 적재됐던 날짜도 직전 영업일 대비 Δ로 보정). 휴장일은 빈 응답으로 자동 스킵.
- `--resume`(기본)으로 이미 쌓인 날짜는 건너뛰어, 길게 돌리다 끊겨도 이어서 진행됩니다.
- 순유입은 **직전 '데이터가 있는' 영업일** 대비로 계산되어 연휴 사이도 올바르게 이어집니다.

---

> 📌 **ER 다이어그램 + 컬럼별 상세 데이터 사전**(타입·키·단위·원천·예시)은
> [docs/schema/README.md](docs/schema/README.md)에 별도 정리되어 있습니다. 아래는 요약본.

## 4. 테이블 & 컬럼 사전

### `funds` — 펀드 식별정보 (날짜 무관, 최신값 유지)
| 컬럼 | 의미 | 출처 |
|---|---|---|
| `fund_code` (PK) | 펀드 표준코드(KR/K55…) | tmpV12 |
| `fund_name` | 펀드명 | tmpV2 |
| `manager` | 운용사 | tmpV1 |
| `fund_type` | 펀드유형 | tmpV3 |
| `vintage_year` | 목표은퇴연도(펀드명에서 파싱, 예 2045) | name |
| `first_seen` / `last_seen` | 최초/최근 관측 시각(UTC) | 적재시각 |

### `nav_daily` — 일별 기준가·규모  *(PK: fund_code+base_date)*
| 컬럼 | 의미 | 단위 | 출처 |
|---|---|---|---|
| `base_date` | 기준일자 | YYYY-MM-DD | tmpV14 |
| `nav` | **기준가격** | 원 | tmpV6 |
| `aum_settlement` | **설정액(설정원본)** | **백만원** | tmpV5 |
| `net_assets` | 순자산총액 | **백만원** | tmpV9 |

### `flows_daily` — 일별 자금흐름(파생)  *(PK: fund_code+base_date)*
| 컬럼 | 의미 | 단위 | 비고 |
|---|---|---|---|
| `net_flow` | **순유입** = 직전 적재일 대비 설정액 증감 | 백만원 | 설정액 Δ로 계산 |
| `inflow` / `outflow` | 설정/해지 개별액 | 백만원 | KOFIA 미제공 → NULL |

### `fees` — 보수(월별 공시)  *(PK: fund_code+base_date)*
| 컬럼 | 의미 | 단위 | 출처 |
|---|---|---|---|
| `base_date` | 보수 적용 월말 기준일 | YYYY-MM-DD | 질의일 주입 |
| `ter_synthetic` | **합성 총보수·비용비율 TER(A+B)** | % | tmpV12 |
| `mgmt_fee` | 운용보수 | % | tmpV5 |
| `sales_fee` | 판매보수 | % | tmpV6 |

### `crawl_runs` — 실행 로그
| 컬럼 | 의미 |
|---|---|
| `run_id` | 실행 일련번호 |
| `run_date` | 대상 base_date |
| `started_at`/`finished_at` | 시작/종료 시각(UTC) |
| `status` | `ok` / `error` / `dry-run` |
| `records_upserted` | upsert 행 수 |
| `error` | 실패 메시지 |

---

## 5. 주의할 데이터 특성

- **단위**: 설정액·순자산은 **백만원**, 기준가는 **원**, 보수는 **%**.
- **순자산(net_assets)=0 인 행**: 재간접형/모(母)펀드 일부 클래스는 이 컬럼이 0으로 공시됨.
  규모 분석은 보통 **설정액(aum_settlement)** 을 사용.
- **보수가 비어있는(NULL) TDF**: 운용(母)·기관전용 클래스는 소매 보수가 없어 보수표에 미포함.
  보수는 보통 **판매클래스(ClassC 등)** 에 존재.
- **보수 시차**: 보수는 매월 공시되어 최신이 1~2개월 전 월말. 크롤러가 자동으로 가장 최근
  데이터 있는 월말로 백워크하여 채움(`fee_date` 로그로 확인).
- **자금유출입**: KOFIA가 일별 설정/해지 금액을 깔끔히 제공하지 않아 **설정액 일간 증감**으로 산출.
  최소 2개 영업일이 적재되어야 값이 생김(첫날은 flows=0).
